import type { SongSet } from '../types';

/** Grid of all songs, chords-only, for a quick structural overview. Deferred. */
export function mountEagleView(_root: HTMLElement, _set: SongSet): void {
  throw new Error('mountEagleView not implemented yet');
}
