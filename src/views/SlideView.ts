import type { SongSet } from '../types';

/** Full-screen, one-song-at-a-time presentation view. Implementation deferred. */
export function mountSlideView(_root: HTMLElement, _set: SongSet): void {
  throw new Error('mountSlideView not implemented yet');
}
