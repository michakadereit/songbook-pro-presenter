{
  "name": "songbook-pro-presenter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WebApp to display and present song sets from SongBook Pro (.sbp).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.0",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "chordsheetjs": "^15.5.1",
    "fflate": "^0.8.3"
  }
}
